%*****************************************************************************************************************************************%
%********************************************************* `my-listings.sty' *************************************************************%
%*****************************************************************************************************************************************%
% This is an adaptation of the 'glossaries' package's 'glossary-hypernav.sty' component implementing the 'indexhypergroup' glossary style selected using the
% \setglossarystyle{indexhypergroup} command
%*****************************************************************************************************************************************%
%*************************************** Define provided package name and LaTeX/package dependencies *************************************%
%*****************************************************************************************************************************************%
\ProvidesPackage{my-listings}
%\usepackage[procnames]{listingsutf8}
%*****************************************************************************************************************************************%
%****************************************** Package options and corresponding Boolean variables ******************************************%
%*****************************************************************************************************************************************%
%-----------------------------------------------------------------------------------------------------------------------------------------%
\DeclareOption*{\PackageWarning{my-listings}{Unknown \CurrentOption}}
%*****************************************************************************************************************************************%
%***************************************************** Processing of package options *****************************************************%
%*****************************************************************************************************************************************%
%-----------------------------------------------------------------------------------------------------------------------------------------%
\ProcessOptions\relax
%-----------------------------------------------------------------------------------------------------------------------------------------%
%\catcode`\!=12
%\makeatletter							% Change @ to standard letter, \catcode'@=11 so it can be used in macro names/definition
%*****************************************************************************************************************************************%
%************************************************************* Package Body **************************************************************%
%*****************************************************************************************************************************************%
\def\@tcblistings@rulesep{0.75pt}							%
\def\@tcblistings@framerule{2.0pt}							%
\def\@tcblistings@framesep{1.0mm}
\def\@tcblistings@numbersep{15.0pt}
\def\@tcblistings@framexleftmargin{4.0pt}
\def\@tcblistings@framexrightmargin{-3.0mm}
\def\@tcblistings@framextopmargin{0.5mm}
\def\@tcblistings@framexbottommargin{0.5mm}
\def\@tcblistings@linewidth{17.25cm}
\def\@tcblistings@boxsep{3.0pt}
\def\@tcblistings@boxrule{2.0pt}
\def\@tcblistings@width{\dimexpr\@tcblistings@linewidth + \@tcblistings@numbersep + 2\@tcblistings@rulesep + 1.5mm\relax}
\def\@tcblistings@captionwidth{\dimexpr\@tcblistings@linewidth + \@tcblistings@framexleftmargin + 9.5pt\relax}
\def\@tcblistings@captionleftskip{\dimexpr\@tcblistings@framerule+\@tcblistings@framerule+\@tcblistings@rulesep+\@tcblistings@framexleftmargin + 1mm\relax}
\def\@terminal@style@xleftmargin{3pt}
\def\@terminal@style@xrightmargin{-\dimexpr 11.33mm - 3pt\relax}
\def\@terminal@style@rulesep{0.0mm}
\def\@terminal@style@framesep{0.25mm}
\def\@terminal@style@framerule{0.25mm}
\def\@terminal@style@framexleftmargin{5pt}
\def\@terminal@style@framexrightmargin{1pt}
\def\@terminal@style@framextopmargin{3mm}
\def\@terminal@style@framexbottommargin{3mm}
\def\@terminal@style@aboveskip{0.95pt}
\def\@terminal@style@belowskip{-\dimexpr 6.25mm - 0.5pt\relax}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%-----------------------------------------------------------------------------------------------------------------------------------------%
%-----------------------------------------------------------------------------------------------------------------------------------------%
%\DeclareCaptionType{code}[Code Listing][List of Code Listings]
\DeclareCaptionFont{white}{ \color{white} }
\DeclareCaptionFont{black}{ \color{black} }
\DeclareCaptionFont{blue}{ \color{blue} }
\DeclareCaptionFont{green}{ \color{green} }
%\DeclareCaptionLabelFormat{emptylabel}{\bothIfFirst{#1}{}#2}
%\DeclareCaptionFormat{listing}{#1#2#3\par}
%\DeclareCaptionFormat{empty}{ }
\DeclareCaptionFormat{listing}
{
	\begin{tcolorbox}[tcblistingcaptionStyle]
		\unskip\ignorespaces\parbox{\textwidth}{#1#2#3}
	\end{tcolorbox}
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
\captionsetup[lstlisting]
{%
	format=listing, labelfont={sf,white}, textfont={white,sf}, singlelinecheck=false, width=\linewidth,
	margin={0.0cm,-1.0cm}, justification=justified, aboveskip=-2.5mm, belowskip=0.5mm
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%-----------------------------------------------------------------------------------------------------------------------------------------%
%-----------------------------------------------------------------------------------------------------------------------------------------%
\DeclareTotalTCBox{\commandbox}{s v}{verbatim,colupper=white,colback=black!75!white,colframe=black}
{%
	\IfBooleanTF{#1}{\textcolor{red}{\ttfamily\bfseries > }}{}%
	\lstinline[language=command.com, keywordstyle=\color{blue!35!white}\bfseries]^#2^%
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
\DeclareTotalTCBox{\tcbinlinebashbox}{s t! v}{tcbinlinebashboxStyle}
{%
     	\IfBooleanTF{#1}{\raisebox{\dimexpr-0.25\textsize\relax}{\rule{0pt}{\textsize}}\textcolor{green}{ > }}{}%
    	\IfBooleanTF{#2}
    	{
	    	\lstinline
		[
	     		style=emptyStyle, language=bash-extended,  name=bash, identifierstyle=\textcolor{white}, commentstyle=\textcolor{white},
			stringstyle=\color{white}\upshape, showstringspaces=true, keywordstyle={[3]\color{white}},	     		
			keywordstyle={[1]\color{white}}, keywordstyle={[2]\color{white}}
	     	]^#3^%
     	}
     	{
     		\lstinline
		[
	     		language=bash-extended,  name=bash, showstringspaces=true
	     	]^#3^%
     	}
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%                                                   1 2  3 4
\DeclareTCBListing[blend into=listings]{tcbbashbox}{s t! O{} d()}
{%
	%width=\linewidth,
	blend before title={colon},
	blend before title code={\hspace{-2.5mm}},
	IfBooleanTF={#1}
		{tcbbashboxStyle={stdblue}{green}}
		{tcbbashboxStyle},	
	%code={\renewcommand{\lstlistingname}{}},%\setstretch{0.5}%
	code=
		{
			\renewcommand{\lstlistingname}{\@tcblisting@terminal@caption@title}%
			\if\relax\detokenize{#3}\relax	
				\def\@title@text{}%\def\@title@sep{}%
		    	\else
		    		\def\@title@text{\space:\space#3}%\def\@title@sep{\space:\space}%
			\fi
		},
	IfValueTF={#3}
	{
		%code={\def\@title@text{\shadowText|\sffamily|[black](white){\space:\space#3}}},
		IfBooleanTF={#2}
			{title={\shadowText|\sffamily|[black](white){\@tcblisting@terminal@caption@title\ \theTerminallisting\@title@text}}}
			{title={\shadowText|\sffamily|[black](white){\@tcblisting@terminal@caption@title\ \theTerminallisting\@title@text}}}
	}
	{
		%code={\def\@title@text{\shadowText|\sffamily|[black](white){}}},
		IfBooleanTF={#2}
			{title={}}
			{title={\shadowText|\sffamily|[black](white){\@tcblisting@terminal@caption@title\ \theTerminallisting }}}
	},
	%list text={\normalsize\bfseries\protect\numberline{\thelstlisting}\normalsize\bfseries\@tcblisting@terminal@caption@title\ \theTerminallisting },
	%list entry={\protect\numberline{\hspace{2.0em}\lstlistingname\ \thelstlisting }},
	list entry={\normalsize\bfseries\protect\numberline{\thelstlisting}\@tcblisting@terminal@caption@title\ \theTerminallisting\@title@text},
	IfValueTF={#4}
		{comment={#4}}
		{listing only},
	IfValueTF={#3}{}{top=3.0mm, colback=black},
	code={\refstepcounter{Terminallisting}\par\medskip}
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%% Usage: \lstMatlab{number the first line as}{first line in file}{last line in file}{caption}{label}{file}
%\newcommand{\lstMatlab}[6]{
%                                1 2 3 4 5 6
\newtcbinputlisting[blend into=listings]{\Matlab}[7][]
	{
		MatlabStyle,
		code=
		{
			\renewcommand{\lstlistingname}{\@tcblisting@Matlab@caption@title}%
			\if\relax\detokenize{#1}\relax	
				\def\@title@sep{}%
		    	\else
		    		\def\@title@sep{\space:\space}%
			\fi
		},
		listing file={#7},
		blend before title={colon},
		blend before title code={\hspace{-10mm}\BreakableShadowText{\sffamily}<0.65pt><0.2pt>[white](black){\@tcblisting@Matlab@caption@title\ \theMatlablisting\@title@sep}},
		%list text={\normalsize\bfseries\protect\numberline{\thelstlisting}\@tcblisting@Matlab@caption@title\ \theMatlablisting },
		list entry={\normalsize\bfseries\protect\numberline{\thelstlisting}\@tcblisting@Matlab@caption@title\ \theMatlablisting\@title@sep#1},
		title={\addtocounter{lstlisting}{-1}\BreakableShadowText{\sffamily}<0.65pt><0.2pt>[white](black){#1}},
		listing options=
		{
	   		style=codingStyle, language=matlab-extended, caption={#5}, label={#6},
			firstnumber={#2}, firstline={#3}, lastline={#4}					%			
		},
		code={\refstepcounter{Matlablisting}}
	}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%% Usage: \cplusplus{number the first line as}{first line in file}{last line in file}{caption}{label}{file}
\newcommand{\cpluspluscode}[6]{
    \captionsetup[lstlisting]{singlelinecheck=false, width=\linewidth, margin={0.0cm,-1.0cm},justification=justified}
    \lstinputlisting[style=codingStyle, language=C++, firstnumber={#1}, firstline={#2}, lastline={#3}, caption={#4}, label={#5}]{#6}
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%% Usage: \ccode{number the first line as}{first line in file}{last line in file}{caption}{label}{file}
\newcommand{\ccode}[6]{
    \captionsetup[lstlisting]{singlelinecheck=false, width=\linewidth, margin={0.0cm,-1.0cm},justification=justified}
    \lstinputlisting[style=codingStyle, language=C, firstnumber={#1}, firstline={#2}, lastline={#3}, caption={#4}, label={#5}]{#6}
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%% Usage: \Verilog{number the first line as}{first line in file}{last line in file}{caption}{label}{file}
\newcommand{\Verilogcode}[6]{
    \captionsetup[lstlisting]{singlelinecheck=false, width=\linewidth, margin={0.0cm,-1.0cm},justification=justified}
    \lstinputlisting[style=codingStyle, language=Verilog, firstnumber={#1}, firstline={#2}, lastline={#3}, caption={#4}, label={#5}]{#6}
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%% Usage: \Java{number the first line as}{first line in file}{last line in file}{caption}{label}{file}
\newcommand{\Javacode}[6]{
    \captionsetup[lstlisting]{singlelinecheck=false, width=\linewidth, margin={0.0cm,-1.0cm},justification=justified}
    \lstinputlisting[style=codingStyle, language=Java, firstnumber={#1}, firstline={#2}, lastline={#3}, caption={#4}, label={#5}]{#6}
}
\lstdefinelanguage{bash-extended}
{
	%language={command.com},
	language={bash}, name=bash, float, floatplacement=b, commentstyle=\color{bash-extended-commentcolor}\textit,
	alsodigit={-},							% add the minus sign '-' to the list of identifier digits so they will be printed
	alsoletter={-},
	%morekeywords={cp, rm, mv, git},
    	morekeywords=                           % if you want to add more keywords to the set
    	{[1]
        	cp, rm, mv, git, mkdir, rmdir
    	},
    	keywordstyle={[1]\bfseries\color{bash-extended-keywordcolor1}},						% keyword style
   	morekeywords=                           % if you want to add more keywords to the set
    	{[2]
        	pull, push, fetch, clone, status, remote, add, commit, init, diff, reset, stash, branch, checkout, merge, rm, mv, log, tag,
    	},
    	keywordstyle={[2]\bfseries\color{bash-extended-keywordcolor2}},	
    	morekeywords=                           % if you want to add more keywords to the set
    	{[3]
        	--rebase
    	},
    	keywordstyle={[3]\bfseries\color{bash-extended-keywordcolor3}},	
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
\lstdefinelanguage{git}
{
	%language={command.com},
	name=git, float, floatplacement=b, commentstyle=\color{git-commentcolor}\textit,
	classoffset=0,
   	otherkeywords={--rebase, --flag},
   	%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
   	keywords=                           % if you want to add more keywords to the set
    	{
        	cp, rm, mv, git%, --rebase
    	},
    	keywordstyle={\bfseries\color{git-keywordcolor1}},						% keyword style
   	%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
   	classoffset=1,
   	keywords=                           % if you want to add more keywords to the set
    	{
        	pull, push, fetch, clone, status, remote, add, commit,
        	init, diff, reset, stash, branch, checkout, merge, rm,
        	mv, log, tag%,--rebase%,--flag
    	},
    	keywordstyle={\bfseries\color{git-keywordcolor3}},	
    	%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
   	classoffset=2,
  	keywords={--rebase, --flag},
   	keywordstyle={\bfseries\color{git-keywordcolor4}},	
    	classoffset=0
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
\lstdefinelanguage{matlab-extended}
{
	language=Matlab,
	name=Matlab,
	numberfirstline=true,
	float,
	floatplacement=t,
	mathescape=false,
	escapebegin=\color{matlab-extended-escapebegin},
	escapeend=\color{matlab-extended-escapeend},
	escapeinside={\%*}{*)},
	%framesep=\@tcblistings@framesep,					
    	%frameshape={RYRYNYYYY}{yny}{yny}{RYRYNYYYY},
    	%framexleftmargin=\@tcblistings@framesep,
    	%framexrightmargin=0mm,
    	%escapechar={},
	%abovecaptionskip=-0.5mm,						% is the vertical space respectively above each caption.
	%belowcaptionskip=0mm,						% is the vertical space respectively below each caption.	
	aboveskip=1.5mm,
	belowskip=0mm,
	lineskip=0pt,
	%nolol=false,
	%fancyvrb=true,
  	commentstyle=\color{matlab-extended-commentcolor}\textit,    			% comment style
   	numberstyle=\tiny\bfseries\color{matlab-extended-numbercolor},		% the style that is used for the line-numbers
  	stringstyle=\upshape\color{matlab-extended-stringcolor},     			% string literal style
   	%keywordsprefix={default},
	otherkeywords={(x)},
	deletekeywords=									% deleting so can be added/formatted as desired in another keyword class
	{[1]
		if, for, end, while, do, switch, parfor, return, continue, break, pause, try, catch, elseif, else, function, classdef, properties,
		events, methods, enumeration, gcf, gca, varargin, varargout, nargin, nargout, narginchk, nargoutchk, filebrowser, workspace, format,
		evalin, assignin, saveas, save, load, delete
	},
	%classoffset=1,
	keywordstyle={[1]\bfseries\color{matlab-extended-keywordcolor1}},						% keyword style
   	morekeywords=                           % if you want to add more keywords to the set
	{[2]
    		plotspec, tf, feedback, step, double, solve, ss, ss2tf, pzmap, lsim, xlim, ylim, sgrid, stepinfo, stepplot, sym, damp, texlabel, rlocus,
		rlocusplot, linmod, tfdata, stepDataOptions, mtit, fir1, freqz, srrc, plotspec2, myeye, interpsinc, quantalph, letters2pam, pam2letters,
		simplify, expand, syms, bode, bodeplot, string, strcat, int8, uint8, jinc, cell, randi, randn, normpdf, normcdf, inputParser,
		parsecreateDatatip, datacursormode, findall, saveas, save, addRequired, addOptional, addParameter, Datatips, parse
    	},
    	keywordstyle={[2]\bfseries\color{matlab-extended-keywordcolor2}},						% keyword style
   	morekeywords=                           % if you want to add more keywords to the set
    	{[3]
        	PlotProps, SDA, optimum_filter, NewtonAlg, run_init, print2PNG, model2PSD, add_hline, add_tick, add_valline, add_xyline, check4exception,
        	genfig, initVals2VarsMap, mapArgs2Vals, mapStrs2Vars, mapVals2Vars, model2spectrum
    	},
    	keywordstyle={[3]\bfseries\color{matlab-extended-keywordcolor3}},						% keyword style
   	morekeywords=                           % if you want to add more keywords to the set
    	{[4]
        	MyConstants, latex, LaTeX, Latex, 'latex', 'LaTeX', 'Latex', gcf, gca
    	},
    	keywordstyle={[4]\bfseries\color{matlab-extended-keywordcolor4}},						% keyword style
   	morekeywords=                           % if you want to add more keywords to the set
    	{[5]
        	function, classdef, properties, events, methods, enumeration
    	},
    	keywordstyle={[5]\bfseries\color{matlab-extended-keywordcolor5}},						% keyword style
   	morekeywords=                           % if you want to add more keywords to the set
    	{[6]
        	if, for, end, while, do, switch, parfor, return, continue, break, pause, try, catch, elseif, else
    	},
    	keywordstyle={[6]\bfseries\color{matlab-extended-keywordcolor6}},						% keyword style
   	morekeywords=                           % if you want to add more keywords to the set
    	{[7]
        	true, false, TRUE, FALSE, definedColors, executionModes, defaultExecMode, NavyBlue
    	},
	keywordstyle={[7]\bfseries\color{matlab-extended-keywordcolor7}},						% keyword style
	morekeywords=                           % if you want to add more keywords to the set
    	{[8]
        	@, @ischar, @isnumeric, @Datatips, varargin, varargout, nargin, nargout, narginchk, nargoutchk
    	},
	keywordstyle={[8]\bfseries\color{matlab-extended-keywordcolor8}},						% keyword style
	morekeywords=                           % if you want to add more keywords to the set
    	{[9]
        	 FunctionName, KeepUnmatched, PartialMatching, CaseSensitive, StructExpand, Results, long, compact
    	},
	keywordstyle={[9]\bfseries\color{matlab-extended-keywordcolor9}},							% keyword style
	morekeywords=                           % if you want to add more keywords to the set
    	{[10]
        	 filebrowser, workspace, format, evalin, assignin, load, delete
    	},
	keywordstyle={[10]\bfseries\color{matlab-extended-keywordcolor10}}							% keyword style
	%index={},
    	%indexstyle={},
    	%tagstyle={},
	%procnamekeys={function, classdef, properties},				%
    	%procnamestyle=\bfseries,							%
}
\lstdefinelanguage{diff}
{
  sensitive=true,
  % diff command line
  morecomment=[f][\color{gray}][0]{diff},
  % commit identifiers for git diff
  morecomment=[f][\color{gray}][0]{index},
  % hunk location/line numbers for unified format
  morecomment=[f][\color{blue}][0]{@@},
  % hunk location/line numbers for context format
  morecomment=[f][\color{magenta}][0]{***},
  % changed line for context format
  morecomment=[f][\color{violet}][0]{!},
  % deleted lines for unified format
  morecomment=[f][\color{red!60!black}][0]-,
  % added lines for unified format
  morecomment=[f][\color{green!60!black}][0]+,
  % file name and time stamp old file
  morecomment=[f][\color{magenta}][0]{---},
  % file name and time stamp new file
  morecomment=[f][\color{magenta}][0]{+++},
  % Binary files ... differ
  morecomment=[f][\color{gray}][0]{Binary},
  % Only in ...: file.txt
  morecomment=[f][\color{gray}][0]{Only},
  % old mode ...
  morecomment=[f][\color{gray}][0]{old},
  % new mode ...
  morecomment=[f][\color{gray}][0]{new},
  % rename from/to ...
  morecomment=[f][\color{gray}][0]{rename},
  % similarity index ...%
  morecomment=[f][\color{gray}][0]{similarity},
  % deleted file mode ...%
  morecomment=[f][\color{gray}][0]{deleted},
  % hunk separator for context format
  morecomment=[f][\color{magenta}][0]{***************},
  % deleted lines for normal format
  morecomment=[f][\color{red!60!black}][0]<,
  % added lines for normal format
  morecomment=[f][\color{green!60!black}][0]>,
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{0},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{1},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{2},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{3},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{4},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{5},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{6},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{7},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{8},
  % line number specifier for normal format
  morecomment=[f][\color{blue}][0]{9},
}[comments]
%\lstdefineformat{C}{%
%\{=\newline\string\newline\indent,%
%\}=\newline\noindent\string\newline,%
%;=[\ ]\string\space}
%\lstMakeShortInline[[options]]hharacter
\lstdefinestyle{codingStyle}
{
    	abovecaptionskip=-0.5mm,						% is the vertical space respectively above each caption.
	backgroundcolor=\color{codingStyle-backgroundcolor},	% choose the background color. You must add \RequirePackage{color}
	basewidth={0.575em,0.45em}, 					% default: {fixed,flexible mode}={0.6em,0.45em}
	basicstyle=\normalsize,               			% the size of the fonts that are used for the code
    	belowcaptionskip=0mm,						% is the vertical space respectively below each caption.	
	%boxpos=<=b|c|t>						%bottom baseline, centered or top baseline				%
	breakatwhitespace=false,                			% sets if automatic breaks should only happen at whitespace
    	breakautoindent=true,
    	%breakindent=20pt,
    	breaklines=true,                        			% sets automatic line breaking
  	% caption=\lstname,                       		% can be used instead of title
    	captionpos=b,                           			% sets the caption-position to bottom
    	columns=[c]fixed,						%=[c|l|r]<alignment > (fixed, flexible, spaceflexible, or fullflexible)
    	commentstyle=\color{codingStyle-commentcolor}\textit,    % comment style
    	%deletekeywords={...},                   			% if you want to delete keywords from the given language
    	%emptylines=[*]number						% max printed empty lines, * retains line numbering
    	extendedchars=true,                     			% lets you use non-ASCII characters; for 8-bits encodings only, does not work with UTF-8
    	fancyvrb=true,
    	fillcolor=\color{codingStyle-fillcolor},			%
    	firstnumber=auto,
    	flexiblecolumns=false,
    	float,								% tbph
    	floatplacement=t,							% is used as place specier if float is used without value
	fontadjust=false,			
    	formfeed=\bigbreak, 						% Whenever a listing contains a form feed, <tokens> is executed.
	frame=TRBL,                        				% adds a frame around the code
    	%frameround=tttt,							%
    	framerule=\@tcblistings@framerule,					%
    	framesep=\@tcblistings@framesep,					
    	%frameshape={RYRYNYYYY}{yny}{yny}{RYRYNYYYY},
    	framexleftmargin=\@tcblistings@framexleftmargin,
    	framextopmargin=\@tcblistings@framextopmargin,
    	framexbottommargin=\@tcblistings@framexbottommargin,
    	framexrightmargin=\@tcblistings@framexrightmargin,
    	%framextopmargin=0mm,
    	%framexbottommargin=0mm,
    	%fvcmdparams={},							%=<command1><number1>... \overlay1
    	%gobble=#								% # characters gobbled each line
    	identifierstyle=\color{codingStyle-identifiercolor},	% define style used in printing non-keyword/cs/directive text in code
    	%index=[1][keywords],
    	keepspaces=true,                        			% keeps spaces in text, useful for keeping indentation of code (possibly needs columns=flexible)
    	keywordstyle={[1]\bfseries\color{codingStyle-keywordcolor1}},% keyword style
    	keywordstyle={[2]\bfseries\color{codingStyle-keywordcolor2}},% keyword style
    	keywordstyle={[3]\bfseries\color{codingStyle-keywordcolor3}},% keyword style
    	linewidth=\@tcblistings@linewidth,                      	% define width of code listing on page
    	%linerange={first1-last1 i,first2-last2, ...}         % The given line ranges of the (only for) individual listing are displayed.
	%lineskip=<dimension>,                      		% species additional space between lines in listings
    	%morefvcmdparams={},						%=<command1><number1>... \overlay1
    	numberblanklines=true,						% If this is set to false, blank lines get no printed line number.
    	numberfirstline=false,						% Sets numbering of first line of code:=<auto|last|number>
    	numbers=left,                           			% where to put the line-numbers
    	numbersep=\@tcblistings@numbersep,                          			% how far the line-numbers are from the code
    	numberstyle=\tiny\bfseries\color{codingStyle-numbercolor},% the style that is used for the line-numbers
    	resetmargins=false,
	rulecolor=\color{codingStyle-rulecolor},                 % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. commens (green here))
    	rulesepcolor=\color{codingStyle-rulesepcolor},		%
    	rulesep=\@tcblistings@rulesep,					%
    	sensitive=true,							% makes keywords, control sequences, and directives case sensitive
    	showspaces=false,                       			% show spaces adding particular underscores
    	showstringspaces=false,                 			% underline spaces within strings
    	showtabs=false,                         			% show tabs within strings adding particular underscores
    	stepnumber=1,                           			% the step between two line-numbers. If it's 1, each line will be numbered
    	stringstyle=\upshape\color{codingStyle-stringcolor},     % string literal style
    	tab=$\to$,								%
    	tabsize=2,                             			% sets default tabsize to 2 spaces
    	%texcl=false,							% Activate/deactivate TeX comment lines inside code after escapeinside activated
    	%title=\lstname,                        			% show the filename of files included with \lstinputlisting; also try caption instead of title
    	upquote=true							% determines whether the left and right quote are printed  or `' (requires 'textcomp' package if true		
    	%xleftmargin=2mm,
    	%xrightmargin=-0.25cm
    	%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    	%directives={},							% ERROR list of compiler directives
	%inputencoding=t1enc,						% ERROR determines the input encoding; requires 'inputenc' package if true
	%texcs={},								% ERROR list of control sequences
    	%usekeywordsintag=true,						% ERROR
    	%markfirstintag=false, 						% ERROR
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
\lstdefinestyle{emptyStyle}
{
    	abovecaptionskip=0mm,						% is the vertical space respectively above each caption.
	aboveskip=3.5mm,
	alsodigit={-},
	alsoletter={-},
	backgroundcolor=\color{emptyStyle-backgroundcolor},	% choose the background color. You must add \RequirePackage{color}
	basewidth={0.575em,0.45em}, 					% default: {fixed,flexible mode}={0.6em,0.45em}
	basicstyle=\ttfamily,               			% the size of the fonts that are used for the code
    	belowcaptionskip=0mm,						% is the vertical space respectively below each caption.	
	belowskip=-3mm,
	breakatwhitespace=false,                			% sets if automatic breaks should only happen at whitespace
    	breakautoindent=true,
    	breakindent=20pt,
    	breaklines=true,                        			% sets automatic line breaking
  	% caption=\lstname,                       		% can be used instead of title
    	captionpos=b,                           			% sets the caption-position to bottom
    	columns=[c]fixed,						%=[c|l|r]<alignment > (fixed, flexible, spaceflexible, or fullflexible)
    	commentstyle=\color{emptyStyle-commentcolor}\textit,    % comment style
    	%deletekeywords={...},                   			% if you want to delete keywords from the given language
    	escapebegin=\color{emptyStyle-escapebegin},
	escapeend=\color{emptyStyle-escapeend},
	escapeinside={\%*}{*)}, %escapechar={},
	extendedchars=true,                     			% lets you use non-ASCII characters; for 8-bits encodings only, does not work with UTF-8
    	fancyvrb=true,
    	fillcolor=\color{emptyStyle-fillcolor},			%
    	firstnumber=auto,
    	flexiblecolumns=false,
    	float,
    	floatplacement=t,
	fontadjust=false,			
    	formfeed=\bigbreak, 						% Whenever a listing contains a form feed, <tokens> is executed.
	frame=TRBL,                        				% adds a frame around the code
    	%frameround=tttt,							%
    	framerule=0mm,					%
    	framesep=0mm,					
    	%frameshape={RYRYNYYYY}{yny}{yny}{RYRYNYYYY},
    	framexleftmargin=0mm,
    	%framexrightmargin=0mm,
    	%framextopmargin=0mm,
    	%framexbottommargin=0mm,
    	%fvcmdparams={},							%=<command1><number1>... \overlay1
    	identifierstyle=\color{emptyStyle-identifiercolor},	% define style used in printing non-keyword/cs/directive text in code
    	%index=[1][keywords],
    	keepspaces=true,                        			% keeps spaces in text, useful for keeping indentation of code (possibly needs columns=flexible)
    	keywordstyle={[1]\bfseries\color{emptyStyle-keywordcolor1}},% keyword style
    	keywordstyle={[2]\bfseries\color{emptyStyle-keywordcolor2}},% keyword style
    	keywordstyle={[3]\bfseries\color{emptyStyle-keywordcolor3}},% keyword style
    	lineskip=0pt, %nolol=false, %fancyvrb=true,
	linewidth=\@tcblistings@linewidth,                      	% define width of code listing on page
    	mathescape=false,
	%morefvcmdparams={},						%=<command1><number1>... \overlay1
    	numberblanklines=true,						% If this is set to false, blank lines get no printed line number.
    	numberfirstline=false,						% Sets numbering of first line of code:=<auto|last|number>
    	numbers=none,                           			% where to put the line-numbers
    	numbersep=\@tcblistings@numbersep,                          			% how far the line-numbers are from the code
    	numberstyle=\tiny\bfseries\color{emptyStyle-numbercolor},% the style that is used for the line-numbers
    	resetmargins=false,
	rulecolor=\color{emptyStyle-rulecolor},                 % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. commens (green here))
    	rulesepcolor=\color{emptyStyle-rulesepcolor},		%
    	rulesep=0mm,					%
    	sensitive=true,							% makes keywords, control sequences, and directives case sensitive
    	showspaces=false,                       			% show spaces adding particular underscores
    	showstringspaces=false,                 			% underline spaces within strings
    	showtabs=true,                         			% show tabs within strings adding particular underscores
    	stepnumber=1,                           			% the step between two line-numbers. If it's 1, each line will be numbered
    	stringstyle=\upshape\color{emptyStyle-stringcolor},     % string literal style
    	tab=\unskip\ignorespaces,								%
    	tabsize=1,                             			% sets default tabsize to 2 spaces
    	%texcl=false,							% Activate/deactivate TeX comment lines inside code after escapeinside activated
    	%title=\lstname,                        			% show the filename of files included with \lstinputlisting; also try caption instead of title
    	upquote=true							% determines whether the left and right quote are printed  or `' (requires 'textcomp' package if true		
    	%xleftmargin=2mm,
    	%xrightmargin=-0.25cm
    	%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    	%directives={},							% ERROR list of compiler directives
	%inputencoding=t1enc,						% ERROR determines the input encoding; requires 'inputenc' package if true
	%texcs={},								% ERROR list of control sequences
    	%usekeywordsintag=true,						% ERROR
    	%markfirstintag=false, 						% ERROR
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
\lstdefinestyle{terminalStyle}
{
	xleftmargin=\@terminal@style@xleftmargin,							%
    	xrightmargin=\@terminal@style@xrightmargin,
    	framerule=\@terminal@style@framerule,							%   .25 inv
    	rulesep=\@terminal@style@rulesep,							%   .25 inv
    	framesep=\@terminal@style@framesep,					
    	fillcolor=\color{terminalStyle-fillcolor},			%terminalStyle-fillcolor
    	%frameshape={RYRYNYYYY}{yny}{yny}{RYRYNYYYY},
    	framexleftmargin=\@terminal@style@framexleftmargin,
    	framexrightmargin=\@terminal@style@framexrightmargin,
    	%framextopmargin=\@terminal@style@framextopmargin,
    	%framexbottommargin=\@terminal@style@framexbottommargin,
    	aboveskip=\@terminal@style@aboveskip,							% skip ABOVE floating listing content box		
	belowskip=\@terminal@style@belowskip,							% skip BELOW floating listing content box
	abovecaptionskip=0mm,						% is the vertical space respectively above each caption.
	alsodigit={-},							% add the minus sign '-' to the list of identifier digits so they will be printed
	backgroundcolor=\color{terminalStyle-backgroundcolor},% choose the background color. You must add \RequirePackage{color}
	basewidth={0.575em,0.45em}, 					% default: {fixed,flexible mode}={0.6em,0.45em}
	basicstyle=\ttfamily,               			% the size of the fonts that are used for the code
    	belowcaptionskip=0mm,						% is the vertical space respectively below each caption.	
	breakatwhitespace=false,                			% sets if automatic breaks should only happen at whitespace
    	breakautoindent=true,
    	breakindent=20pt,
    	breaklines=true,                        			% sets automatic line breaking
  	% caption=\lstname,                       		% can be used instead of title
    	captionpos=b,                           			% sets the caption-position to bottom
    	columns=[c]fixed,						%=[c|l|r]<alignment > (fixed, flexible, spaceflexible, or fullflexible)
    	commentstyle=\color{terminalStyle-commentcolor}\textit,    % comment style
    	%deletekeywords={...},                   			% if you want to delete keywords from the given language
    	escapebegin=\color{terminalStyle-escapebegin},
	escapeend=\color{terminalStyle-escapeend},
	escapeinside={\%*}{*)}, %escapechar={},
	extendedchars=true,                     			% lets you use non-ASCII characters; for 8-bits encodings only, does not work with UTF-8
    	fancyvrb=true,
    	firstnumber=auto,
    	flexiblecolumns=false,
    	float,
    	floatplacement=t,
	fontadjust=false,			
    	formfeed=\bigbreak, 						% Whenever a listing contains a form feed, <tokens> is executed.
	frame=TRBL,                        				% adds a frame around the code
    	%frameround=tttt,							%
    	%fvcmdparams={},							%=<command1><number1>... \overlay1
    	identifierstyle=\color{terminalStyle-identifiercolor},	% define style used in printing non-keyword/cs/directive text in code
    	%index=[1][keywords],
    	keepspaces=true,                        			% keeps spaces in text, useful for keeping indentation of code (possibly needs columns=flexible)
    	keywordstyle={[1]\bfseries\color{terminalStyle-keywordcolor1}},% keyword style
    	keywordstyle={[2]\bfseries\color{terminalStyle-keywordcolor2}},% keyword style
    	keywordstyle={[3]\bfseries\color{terminalStyle-keywordcolor3}},% keyword style
    	lineskip=0pt, %nolol=false, %fancyvrb=true,
	linewidth=\@tcblistings@linewidth,                      	% define width of code listing on page
    	mathescape=false,
	%morefvcmdparams={},						%=<command1><number1>... \overlay1
    	numberblanklines=true,						% If this is set to false, blank lines get no printed line number.
    	numberfirstline=false,						% Sets numbering of first line of code:=<auto|last|number>
    	numbers=none,                           			% where to put the line-numbers
    	numbersep=\@tcblistings@numbersep,                          			% how far the line-numbers are from the code
    	numberstyle=\tiny\bfseries\color{terminalStyle-numbercolor},% the style that is used for the line-numbers
    	resetmargins=false,
	rulecolor=\color{terminalStyle-rulecolor},                 % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. commens (green here))
    	rulesepcolor=\color{terminalStyle-rulesepcolor},		%
    	sensitive=true,							% makes keywords, control sequences, and directives case sensitive
    	showspaces=false,                       			% show spaces adding particular underscores
    	showstringspaces=false,                 			% underline spaces within strings
    	showtabs=true,                         			% show tabs within strings adding particular underscores
    	stepnumber=1,                           			% the step between two line-numbers. If it's 1, each line will be numbered
    	stringstyle=\upshape\color{terminalStyle-stringcolor},     % string literal style
    	tab=\unskip\ignorespaces,								%
    	tabsize=1,                             			% sets default tabsize to 2 spaces
    	%texcl=false,							% Activate/deactivate TeX comment lines inside code after escapeinside activated
    	%title=\lstname,                        			% show the filename of files included with \lstinputlisting; also try caption instead of title
    	upquote=true							% determines whether the left and right quote are printed  or `' (requires 'textcomp' package if true		
    	%xleftmargin=-1pt,
%    	xrightmargin=-0.75cm
%    	%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    	%directives={},							% ERROR list of compiler directives
	%inputencoding=t1enc,						% ERROR determines the input encoding; requires 'inputenc' package if true
	%texcs={},								% ERROR list of control sequences
    	%usekeywordsintag=true,						% ERROR
    	%markfirstintag=false, 						% ERROR
}
%-----------------------------------------------------------------------------------------------------------------------------------------%
%*****************************************************************************************************************************************%
%*************************************************** End of 'my-listings.sty' package ****************************************************%
%*****************************************************************************************************************************************%
%-----------------------------------------------------------------------------------------------------------------------------------------%
\endinput
